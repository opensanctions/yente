# Validation Report for yente {{ yente_git_version }}

**Date:** {{ date }}
**Dataset:** `{{ dataset }}`

## Fixtures

| Fixture | Description | Generated From | Records |
|---------|-------------|----------------|---------|
{% for f in fixtures -%}
| `{{ f.name }}` | {{ f.description }} | `{{ f.dataset }}` `{{ f.version }}` | {{ f.records }} |
{% endfor %}

## Results

{% for fixture_name, algorithms in results.items() -%}
### {{ fixture_name }}

| Algorithm | Mean Top Score | Matches | Total | % Matches | ID Recall | Mean ID Rank |
|-----------|---------------|---------|-------|-----------|-----------|--------------|
{% for algo, stats in algorithms.items() -%}
| {{ algo }} | {{ "%.3f"|format(stats.mean_top_score) }} | {{ stats.matches }} | {{ stats.total }} | {{ "%.1f"|format(stats.pct_matches) }}% | {% if stats.id_recall is defined %}{{ "%.1f"|format(stats.id_recall) }}%{% else %}—{% endif %} | {% if stats.mean_id_rank is defined %}{{ "%.2f"|format(stats.mean_id_rank) }}{% else %}—{% endif %} |
{% endfor %}
{% endfor %}
## Environment

**yente:** `{{ yente_git_version }}`

```
{{ pip_freeze }}
```
